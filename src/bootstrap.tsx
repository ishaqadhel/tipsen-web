import * as React from 'react';

const Bootstrap: React.FC = () => {
    return (
        <div>
            <p className='text-red-500'>react app</p>
        </div>
    );
};

export default Bootstrap;
